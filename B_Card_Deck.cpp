#include<bits/stdc++.h>
using namespace std;
 // long long n;
        // cin>>n;
        // vector<int> v,ans;
        // for(int i=0;i<n;i++)
        // {
        //     int x;cin>>x;
        //     v.push_back(x);
        // }
        // vector<int>::iterator it;
        
        // while(v.size()
int main()
{
    int t;cin>>t;
    while(t--)
    {
        // long long n;
        // cin>>n;
        // vector<int> v,ans;
        // for(int i=0;i<n;i++)
        // {
        //     int x;cin>>x;
        //     v.push_back(x);
        // }
        // vector<int>::iterator it;
        
        // while(v.size())
        // {
        //     for(it=max_element(v.begin(),v.end());it<v.end();it++)
        //     {
        //         ans.push_back(v[it]);
        //         v.pop_back();
        //     }
        // }
        // for(int i=0;i<ans.size();i++)
        // {
        //     cout<<ans[i]<<" ";
        // }
        // cout<<endl;

        vector<vector<int> > vec{ { 1, 2, 3 },
                              { 4, 5, 6 },
                              { 7, 8, 9 } };
        cout<<vec[0][1];
    }
    return 0;
}
